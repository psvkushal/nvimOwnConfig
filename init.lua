-- The file structure is kept in lua folder because runtime path look up of lua
-- looks in the lua directory, I guess I need to understand more about require of 
-- lua to get hang of it
require("options")
require("plugins")
